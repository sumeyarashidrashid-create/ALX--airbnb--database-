Sql 
1 .--INNER JOIN : Retrieve all bookings and the respective users who made those bookings 
SELECT b.id , u.id AS user_id ,u.name AS User_name 
FROM booking b 

INNER JOIN users u ON b. user_id =u.id ; 

_ _ _ 2 . LEFT JOIN: Retrieve all properties and their reviews,including proprties and their reviews, including properties that have no reviews 
SELECT p.id AS property_id ,p.name AS review_id , r.comment From properties p 
LEFT JOIN review r ON p.id = r. property-id; 

_ _ _ 3. FULL OUTER JOIN :Retrieve all users and all bookings, 
__ even if the user has no booking or booking is not linked to a user 
SELECT u.id AS user_id ,u.name AS user_name ,b.id AS booking_id 
FROM users u 
FULL OUTER JOIN bookings b ON u.id =b. user _id ; 


